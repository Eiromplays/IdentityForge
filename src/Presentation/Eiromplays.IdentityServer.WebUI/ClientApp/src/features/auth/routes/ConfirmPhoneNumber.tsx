import { ConfirmPhoneNumberForm } from '../components/ConfirmPhoneNumberForm';
import { Layout } from '../components/Layout';

export const ConfirmPhoneNumber = () => {
  return (
    <Layout title="Confirm your phone number">
      <ConfirmPhoneNumberForm />
    </Layout>
  );
};
