export type ErrorMessage = {
  displayMode: string;
  uiLocales: string;
  error: string;
  errorDescription: string;
  requestId: string;
  redirectUri: string;
  responseMode: string;
  clientId: string;
};
